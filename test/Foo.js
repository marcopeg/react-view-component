
import {expect} from 'chai';

import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-addons-test-utils';

// import {Foo} from '../src/Foo';

describe.skip('Foo', () => {

    // it('should import the element', () => {
    //     expect(Foo).to.be.a('function');
    // });

    // it('should render the element', () => {
    //     let rendered = TestUtils.renderIntoDocument(<Foo />);
    //     let dom = ReactDOM.findDOMNode(rendered);
    //     expect(dom.innerHTML).to.contain('FooDefaultValue');
    // });

    // it('should receive a value', () => {
    //     let rendered = TestUtils.renderIntoDocument(<Foo value={'a123'} />);
    //     let dom = ReactDOM.findDOMNode(rendered);
    //     expect(dom.innerHTML).to.contain('a123');
    // });

});
